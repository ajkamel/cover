<%= div_for(@authors) do |author| %>
    <h2><%= link_to author.name, author_path(author) %></h2>
    <p> <%= author.biography %> </p>
    <% author.selected_books.each do |book| %>
      <%= link_to book_cover(book), book_path(book) %>
    <% end %>
<% end %>
